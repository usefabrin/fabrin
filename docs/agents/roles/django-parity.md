# Django parity

Read `AGENTS.md` and `docs/DJANGO_PARITY.md`. State the problem Django solves,
the idiomatic Go answer, and the trade Fabrin accepts by choosing that answer.
Reject reflection, import-time discovery, panic-as-exception, global settings,
and names meaningful only to Python users unless a documented decision requires
them.

Own only the paths in the task packet. Hand back design input, not a battery
implementation.
