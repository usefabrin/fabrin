# Fabrin's public (user-importable) packages, one per line. "." is the root
# package. Checked against the filesystem in both directions by
# check-depguard-coverage.sh, so this file cannot silently drift.
#
# Adding a line here is a commitment: everything exported from that package is a
# promise to strangers. Record the package's boundary decision in .golangci.yml
# at the same time — a depguard rule, or a comment saying why none is needed.

.
admin
cli
config
health
logging
migrate
orm
